# 控制器说明

发行包只保留六个极小单三角面控制器：全局、头发、面部、皮肤、衣服、后处理。所有 morph 为 `0` 时保持封版默认值。

- 全局：整体亮度、饱和度、亮/暗分支、全局阴影倍率（默认 `0.4`）、雨量。
- 头发：Kajiya 高光、细节、发影、屏幕空间 Rim、顶光和暗线。
- 面部：SDF、RD/AO、唇部高光、面部 Rim。
- 皮肤：暗亮、RD/LUT、SSS、GGX 微高光、ZMDshadow、屏幕空间 Rim。
- 衣服：AO 明暗分离、金属/粗糙度、宽高光、方向性纤维高光、皮革涂层、MatCap/HDR、FGD、法线、雨水。
- 后处理：Bloom、Log2/GT/AgX/Neutral/ACES Tonemap、曝光、白平衡、Dither、锐化（默认关闭）。

控制器名称使用 CP932/JIS 可编码汉字，采用接近中文的短标签，避免旧版 MMD 无法显示中文的问题。
