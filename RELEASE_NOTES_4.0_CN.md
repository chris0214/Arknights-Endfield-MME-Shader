# Endfield MME 4.0

发布日期：2026-08-13

## 主要更新

- GUI 基础贴图改为三态控制：沿用 PMX、手动替换、无基础贴图。
- 手动替换会复制贴图并重写输出 PMX 的贴图表和材质索引，源 PMX 保持不变。
- 支持处理 PMX 中指向文件夹、无效字符或不存在文件的异常基础贴图路径。
- `None` 材质也可以安全替换或移除基础贴图。
- 旧工程中的 `UsePmxBaseTexture` 设置会自动迁移。
- 补齐 `EndfieldHairVisibility_Capture.fxsub` 和 GUI 所需运行时入口文件。

## 使用提示

对于 PMX 中显示为 `textures` 等无效路径的材质，请在 GUI 中选择“手动替换（写入输出 PMX）”并指定正确贴图，或选择“无基础贴图”。生成过程不会修改原始模型。

